/* Entering in a placement for ship.
Scanner input = new Scanner(System.in);
		String place[][] = new String[6][6];
		int total = 0;
		System.out.println("Enter 1 to place ship on a row.");
		total = input.nextInt();
		for (int ship = 0; ship < total; ship++) {
			System.out.println("Enter row you would like to place ship.");
			for (int row = 0; row < placement.length; row++) {
				
				row = input.nextInt();
				System.out.println("Enter 6 numbers for where ship is located on row " + row + " (1 = empty space, 0 = location): ");
			for (int col = 0; col < placement[row].length; col++) {
				if (ship >= 1) {
					placement[row][col] = input.next();
				}
			}
			}
		}
		printGrid(placement);
	}
  */
